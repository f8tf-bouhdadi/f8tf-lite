cat > .github/workflows/data0-smoke.yml <<'EOF'
name: DATA0 Smoke Test
on:
  push:
  pull_request:

jobs:
  data0:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Smoke (PowerShell)
        shell: pwsh
        run: |
          ./modules/DATA/DATA0_RawStore/scripts/smoke.ps1
EOF
