# web/ — Web pipeline (Lite)

Objectif : illustrer une fonction de plateforme Web (routage/parsing/réponse) de façon minimale.

## À venir
- P0 : TCP/bytes (base)
- P1 : HTTP minimal (request → response)
- P2 : Op RPC (op + args + réponse, JSON)
- P3 : Router (method + path → handler) + guards (→ gates)

## Règle (Lite)
Pas de diagrammes complets ici. Uniquement des extraits simplifiés et des preuves exécutables.
web
